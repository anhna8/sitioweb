# Aquí inicia nuestro repositorio

git init
crear archivo
guardar archivo 
git add .
git commit -m ""
pegar los comandos de github:

git remote aff origin 
git branch -m main
git push -u origin main
